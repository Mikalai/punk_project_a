#include "ellipsoid.h"

namespace Punk {
    namespace Engine {
        namespace Math {
            Ellipsoid::Ellipsoid(float rx, float ry, float rz)
                : m_rx(rx), m_ry(ry), m_rz(rz) {}
        }
    }
}
