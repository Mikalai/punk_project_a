#ifndef _H_PUNK_VIRTUAL_RENDERABLE_PROXY
#define _H_PUNK_VIRTUAL_RENDERABLE_PROXY

#include "../../config.h"
//#include "../../system/object.h"
#include "entity.h"

namespace Attributes
{

	/*class PUNK_ENGINE_API Proxy : public Core::Object
	{
	public:
		Proxy();
		explicit Proxy(const Utility::WorldDesc& world_desc, const Utility::ObjectDesc& value);

		void SetData(Core::Object* value ) { m_data = value; }
		Core::Object* AsObject() { return m_data; }
		const Core::Object* AsObject() const { return m_data; }		

		virtual void Save(Core::Buffer* buffer) const;
		virtual void Load(Core::Buffer* buffer);
		virtual ~Proxy();

	private:
		Core::Object* m_data;
	};*/
}

#endif	//	_H_PUNK_VIRTUAL_RENDERABLE_PROXY